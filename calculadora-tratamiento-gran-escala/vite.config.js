/**
 * Reutiliza el Vite único del workspace (`../vite.config.js`): multi-página + hub.
 * Desde esta carpeta: `npm run dev` sirve también el índice en / y la otra app.
 */
export { default } from "../vite.config.js";
