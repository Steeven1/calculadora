repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
        verbose: true
        stages: [pre-commit]
        args:
            #- --config
            #- .gitleaks.toml
            - --verbose
